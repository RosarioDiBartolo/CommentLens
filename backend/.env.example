# Copy to backend/.env; never commit real credentials.
YOUTUBE_API_KEY=your_youtube_data_api_key_here
DJANGO_SECRET_KEY=replace_with_a_generated_secret_key
# Local development only; set False in deployed environments.
DEBUG=True

# Reuse completed analyses for 24 hours; 0 always refreshes YouTube data.
ANALYSIS_CACHE_TTL_SECONDS=86400

# Set kev for real inference, mock for clearly labelled offline fixtures.
DECISION_PROVIDER=disabled
KEV_BASE_URL=
KEV_MODEL=kev-latest
# Bump when replacing the checkpoint behind the same model alias.
KEV_MODEL_VERSION=1
KEV_API_KEY=
KEV_TIMEOUT=30
KEV_ANALYSIS_BUDGET=120
